if SHIELD_KEYPLAYER

config ZMK_KEYBOARD_NAME
    default "keyplayer"
      
endif

if ZMK_BACKLIGHT

config PWM
    default y

config LED_PWM
    default y

endif # ZMK_BACKLIGHT

config ZMK_CHARGE_MONITOR
    bool "Enable TP4056 charge monitoring"
    default y if DT_HAS_COMPAT_ENABLED(zmk_charge_monitor)
    help
      Monitors the TP4056 CHRG pin via GPIO interrupt and logs charging state changes.